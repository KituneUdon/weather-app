import { FC } from 'react';
import RemoveButton from './RemoveButton';

export default {
  component: RemoveButton,
  title: 'atoms/RemoveButton',
};

export const Default: FC = () => <RemoveButton />;
