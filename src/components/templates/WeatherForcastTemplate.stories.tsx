import { FC } from 'react';
import WeatherForcastTemplate from './WeathreForecastTemplate';

export default {
  component: WeatherForcastTemplate,
  title: 'WeatherForcastTemplate',
};

export const Default: FC = () => <WeatherForcastTemplate />;
